ANTHROPIC_API_KEY=
RPC_URL=http://127.0.0.1:8545
CONTRACT_ADDRESS=
SESSION_KEY_PRIVATE_KEY=
ROCK_BURGER_ADDRESS=
EVIL_STORE_ADDRESS=

# Required by `npm run agent` (a mandate must already be registered on-chain).
MANDATE_ID=

# Required by `npm run scenario` only (it registers its own mandate).
OWNER_PRIVATE_KEY=
